import React from "react";

var Terminal = React.createClass({
    render: function() {
        return (
            <div>
                Terminal
            </div>
        );
    }
});

export default Terminal;
